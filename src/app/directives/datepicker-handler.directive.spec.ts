import { DatepickerHandlerDirective } from './datepicker-handler.directive';

describe('DatepickerHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
