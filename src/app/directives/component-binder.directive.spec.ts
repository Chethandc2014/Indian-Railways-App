import { ComponentBinderDirective } from './component-binder.directive';

describe('ComponentBinderDirective', () => {
  it('should create an instance', () => {
    //const directive = new ComponentBinderDirective();
    //expect(directive).toBeTruthy();
  });
});
